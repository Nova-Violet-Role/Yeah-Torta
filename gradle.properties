# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Djava.net.preferIPv4Stack=true
org.gradle.caching=true
android.useAndroidX=true
android.enableJetifier=true
#android.bundle.enableUncompressedNativeLibs=false
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true
kotlin.code.style=official
kotlin.caching.enabled=true
android.nonTransitiveRClass=false
android.nonFinalResIds=false
org.gradle.configuration-cache=true
android.enableR8.fullMode=false
#Added after updating to Gradle 9.1.0
android.defaults.buildfeatures.resvalues=true
android.sdk.defaultTargetSdkToCompileSdkIfUnset=false
android.enableAppCompileTimeRClass=false
android.onlyEnableUnitTestForTheTestedBuildType=false
android.usesSdkInManifest.disallowed=false
android.uniquePackageNames=false
android.dependency.useConstraints=true
android.r8.strictFullModeForKeepRules=false
android.r8.optimizedResourceShrinking=false
android.builtInKotlin=false
android.newDsl=false

# The project path contains a non-ASCII character ("Yeah!Tortä") — that is the product name and is not
# going to change. AGP's path check is a blanket warning-turned-error (b/95744) aimed at toolchains that
# mishandle non-ASCII on Windows; this build already produces both APKs correctly from this exact path,
# so the check is refusing a configuration that demonstrably works. The flag AGP itself documents for
# this situation turns it back into the non-issue it is here.
android.overridePathCheck=true
